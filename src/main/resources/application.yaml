spring:
  application:
    name: axon-server-example
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update

axon:
  serializer:
    general: jackson
    events: jackson
    messages: jackson

logging:
  level:
    org.axonframework.messaging: DEBUG
    org.axonframework.commandhandling: DEBUG
